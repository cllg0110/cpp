#include <iostream>
#include<cstdlib>
#include<cstdio>
#include<ctime>
#include<cstring>
#include<fstream>
#include<conio.h>
#include<windows.h>

using namespace std;


int leerp1()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 1; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) { cout << lineaPre << endl; }
    contaPre++;
    }
        return EXIT_SUCCESS;
}

int leerres1()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 2; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
             contaRes++;
        }

        return EXIT_SUCCESS;
}

int leerp2()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 3; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}


int leerres2()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 4; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        }

        return EXIT_SUCCESS;
}

int leerp3()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 5; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}
int leerres3()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 6; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        }

        return EXIT_SUCCESS;
}
int leerp4()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 7; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}
int leerres4()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 8; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        }

        return EXIT_SUCCESS;
}
int leerp5()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 9; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}
int leerres5()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 10; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        }

        return EXIT_SUCCESS;
}
int leerp6()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 11; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}
int leerres6()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 12; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        }

        return EXIT_SUCCESS;
}
int leerp7()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 13; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}
int leerres7()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 14; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        }

        return EXIT_SUCCESS;
}
int leerp8()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 15; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}
int leerres8()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 16; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        }

        return EXIT_SUCCESS;
}
int leerp9()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 17; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}
int leerres9()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 18; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        }

        return EXIT_SUCCESS;
}
int leerp10()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaPre;
    int contaPre = 1; 
    int maxPre = 19; 
    while(getline(archivo_entrada, lineaPre)) {
    if(maxPre == contaPre) {cout << lineaPre << endl; }
            contaPre++;
        }
return EXIT_SUCCESS;
}
int leerres10()
{
    ifstream archivo_entrada("preguntas.txt");;
    string lineaRes;
        int contaRes = 1; 
        int maxRes = 20; 
        while(getline(archivo_entrada, lineaRes)) {
        if(maxRes == contaRes) {cout << lineaRes << endl; }
                contaRes++;
        

        }
        return EXIT_SUCCESS;
}

